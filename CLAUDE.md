# CLAUDE.md

For any task in this repository:
1. Read `BOOT.md`.
2. Follow `INSTRUCTIONS.md` protocol.
3. Use `WAL.md` as continuation state.

Canonical spec URI base:
`spec://io.github.limebeck.kmp-docker-client/...`
